# Convert to .env
# or build_arm64_cpu, build_arm64_gpu, build_x86_64_cpu, build_x86_64_gpu, 
# arm64_gpu, arm64_cpu, x86_64_cpu, x86_64_gpu
COMPOSE_PROFILES=arm64_cpu 
# Can be changed to "stdlib-dev" to building and using the cloned mojo stdlib.
PIXI_ENVIRONMENT_NAME="default"